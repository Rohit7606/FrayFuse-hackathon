"""Stress detection — produces own_stress ∈ [0, 1] for observable nodes.

Two signals from disclosures that can disagree:
  1. Migration ratio  (ageing table — can be window-dressed)
  2. Late-payment intensity  (MSMED flow — primary, cannot be tidied)
"""
